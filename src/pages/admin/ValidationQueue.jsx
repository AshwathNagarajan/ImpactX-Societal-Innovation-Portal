import { useState } from "react";
import { Link } from "react-router-dom";
import { challenges } from "../../data/mockData.js";
export default function ValidationQueue(){
  const [rows,setRows]=useState(challenges.filter(c=>["Submitted","Under Review"].includes(c.status)));
  const act=(id,status)=>setRows(r=>r.map(x=>x.id===id?{...x,status}:x));
  return <div><h1 className="text-3xl font-bold text-navy">Validation Queue</h1><div className="mt-5 overflow-x-auto rounded-xl border bg-white shadow-sm"><table className="min-w-[980px] w-full text-left text-sm"><thead className="bg-slate-50 text-slate-500"><tr>{["Challenge ID","Title","Category","District","AI Priority Score","Submitted Date","Action"].map(h=><th key={h} className="p-3">{h}</th>)}</tr></thead><tbody>{rows.map(c=><tr key={c.id} className="border-t"><td className="p-3 font-bold text-blue">{c.id}</td><td className="p-3 font-semibold text-navy">{c.title}</td><td className="p-3">{c.category}</td><td className="p-3">{c.district}</td><td className="p-3">{c.priority==="Critical"?94:c.priority==="High"?86:70}</td><td className="p-3">{c.date}</td><td className="p-3"><div className="flex gap-2"><Link to={`/admin/review/${c.id}`} className="rounded-lg border px-3 py-1.5 font-bold text-blue">View</Link><button onClick={()=>act(c.id,"Validated")} className="rounded-lg bg-green px-3 py-1.5 font-bold text-white">Approve</button><button onClick={()=>act(c.id,"Rejected")} className="rounded-lg bg-red-600 px-3 py-1.5 font-bold text-white">Reject</button><button onClick={()=>act(c.id,"Under Review")} className="rounded-lg bg-orange px-3 py-1.5 font-bold text-white">Request Info</button></div></td></tr>)}</tbody></table></div></div>
}
