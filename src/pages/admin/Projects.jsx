import { useState } from "react";
import { projects } from "../../data/mockData.js";
import ProgressBar from "../../components/common/ProgressBar.jsx";
const stages=["Assigned","Research","Prototype","Testing","Pilot","Implementation","Completed"];
export default function Projects(){const [items,setItems]=useState(projects.map((p,i)=>({...p,stage:stages[i%stages.length]})));const move=(id)=>setItems(x=>x.map(p=>p.id===id?{...p,progress:Math.min(100,p.progress+10),stage:stages[Math.min(stages.length-1,stages.indexOf(p.stage)+1)]}:p));return <div><h1 className="text-3xl font-semibold text-navy">Project Tracking</h1><div className="mt-5 overflow-x-auto rounded-2xl border bg-[#08111f]/70 p-4"><div className="mb-5 flex min-w-[1040px] items-center gap-2">{["Challenge","Validated","Assigned","Research","Prototype","Pilot","Implemented","Impact"].map((s,i)=><div key={s} className="flex items-center gap-2"><span className={`rounded-full px-3 py-1 text-xs font-semibold ${i<4?"bg-green/10 text-green":i===4?"impact-gradient text-[#041016]":"bg-slate-100 text-slate-500"}`}>{s}</span>{i<7&&<span className="text-slate-500">→</span>}</div>)}</div><div className="grid min-w-[1120px] grid-cols-7 gap-3">{stages.map(s=><section key={s} className="rounded-2xl border bg-slate-50 p-3"><h2 className="mb-3 font-semibold text-navy">{s}</h2><div className="space-y-3">{items.filter(p=>p.stage===s).map(p=><article key={p.id} className="rounded-2xl border bg-white p-3 shadow-sm"><h3 className="text-sm font-semibold text-navy">{p.title}</h3><p className="mt-1 text-xs text-slate-500">{p.university}</p><div className="mt-3"><ProgressBar value={p.progress}/></div><button onClick={()=>move(p.id)} className="mt-3 rounded-xl border border-blue/30 bg-blue/10 px-3 py-1.5 text-xs font-semibold text-blue">Update progress</button></article>)}</div></section>)}</div></div></div>}
