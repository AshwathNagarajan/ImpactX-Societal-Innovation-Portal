import { useState } from "react";
import { supportTypes } from "../../data/mockData.js";
export default function Partnerships(){const [done,setDone]=useState(false);return <div className="max-w-4xl min-w-0"><h1 className="text-2xl font-semibold text-navy md:text-3xl">Partnership Form</h1>{done&&<p className="mt-4 rounded-lg bg-green/10 p-3 font-semibold text-green">Partnership commitment stored locally for the prototype.</p>}<form onSubmit={e=>{e.preventDefault();setDone(true)}} className="mt-5 rounded-xl border bg-white p-5 shadow-sm"><div className="grid gap-4 lg:grid-cols-2"><label className="text-sm font-semibold text-slate-600">Support Type<select className="mt-1 w-full rounded-lg border px-3 py-2.5">{supportTypes.map(s=><option key={s}>{s}</option>)}</select></label>{["Contribution","Mentor Name","Timeline"].map(l=><label key={l} className="text-sm font-semibold text-slate-600">{l}<input className="mt-1 w-full rounded-lg border px-3 py-2.5"/></label>)}<label className="text-sm font-semibold text-slate-600 lg:col-span-2">Notes<textarea rows="4" className="mt-1 w-full rounded-lg border px-3 py-2.5"/></label></div><button className="mt-5 w-full rounded-lg bg-green px-5 py-3 font-bold text-white sm:w-auto">Confirm Partnership</button></form></div>}
