import { useState } from "react";
import { challenges } from "../../data/mockData.js";
import ChallengeCard from "../../components/challenges/ChallengeCard.jsx";
export default function AssignedChallenges(){const [note,setNote]=useState("");return <div className="min-w-0"><h1 className="text-2xl font-semibold text-navy md:text-3xl">Assigned Challenges</h1>{note&&<p className="mt-4 rounded-lg bg-green/10 p-3 font-semibold text-green">{note}</p>}<div className="mt-5 grid gap-4 xl:grid-cols-2">{challenges.slice(0,6).map(c=><div key={c.id} className="min-w-0"><ChallengeCard challenge={c}/><div className="-mt-2 grid gap-2 rounded-b-xl border border-t-0 bg-white p-3 sm:grid-cols-3"><button onClick={()=>setNote(`${c.title} accepted by your institute.`)} className="rounded-lg bg-green px-3 py-2 text-sm font-bold text-white">Accept challenge</button><button onClick={()=>setNote(`${c.title} rejected with capacity note.`)} className="rounded-lg bg-red-600 px-3 py-2 text-sm font-bold text-white">Reject</button><a href="/institute/proposals" className="rounded-lg bg-blue px-3 py-2 text-center text-sm font-bold text-white">Submit proposal</a></div></div>)}</div></div>}
