import { useState } from "react";
import { challenges } from "../../data/mockData.js";
import ChallengeCard from "../../components/challenges/ChallengeCard.jsx";
export default function AssignedChallenges(){const [note,setNote]=useState("");return <div className="min-w-0"><h1 className="text-3xl font-semibold tracking-tight text-navy md:text-4xl">Assigned Challenges</h1>{note&&<p className="mt-6 rounded-2xl bg-green/10 p-4 font-semibold text-green">{note}</p>}<div className="mt-8 grid gap-6 xl:grid-cols-2">{challenges.slice(0,6).map(c=><div key={c.id} className="min-w-0"><ChallengeCard challenge={c}/><div className="mt-3 grid gap-3 rounded-2xl border bg-white p-4 shadow-sm sm:grid-cols-3"><button onClick={()=>setNote(`${c.title} accepted by your institute.`)} className="rounded-xl bg-green px-3 py-2.5 text-sm font-semibold text-white">Accept challenge</button><button onClick={()=>setNote(`${c.title} rejected with capacity note.`)} className="rounded-xl bg-red-600 px-3 py-2.5 text-sm font-semibold text-white">Reject</button><a href="/institute/proposals" className="rounded-xl bg-blue px-3 py-2.5 text-center text-sm font-semibold text-white">Submit proposal</a></div></div>)}</div></div>}
