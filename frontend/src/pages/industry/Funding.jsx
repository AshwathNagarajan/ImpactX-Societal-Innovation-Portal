import { ResponsiveContainer, BarChart, Bar, XAxis, YAxis, Tooltip, Legend } from "recharts";
import { useImpactData } from "../../hooks/useImpactData.js";
import ChartCard from "../../components/charts/ChartCard.jsx";
export default function Funding(){const { data } = useImpactData(); const funding=data.funding||{}; const format=(value)=>`₹${((Number(value)||0)/10000000).toFixed(1)} Cr`; const cards=[["Committed",format(funding.committed),"approved funding capacity"],["Utilized",format(funding.utilized),`${funding.utilization_percent||0}% utilization`],["Available",format(funding.available),"remaining allocation"],["Funded Projects",String(funding.funded_projects||0),"database-linked projects"]]; const chart=(funding.by_industry||[]).map(item=>({name:item.name,committed:item.committed/10000000,utilized:item.utilized/10000000})); return <div className="min-w-0"><h1 className="text-3xl font-semibold tracking-tight text-navy md:text-4xl">Funding</h1><p className="mt-3 max-w-3xl text-sm leading-7 text-slate-600">Funding is calculated from industry records and linked project support requirements, so committed, utilized and available values stay tied to database state.</p><div className="mt-8 grid gap-6 md:grid-cols-2 xl:grid-cols-4">{cards.map(([title,value,note])=><div key={title} className="premium-card rounded-2xl border p-6"><p className="text-sm font-semibold text-slate-500">{title}</p><p className="mt-3 text-2xl font-semibold text-navy md:text-3xl">{value}</p><p className="mt-2 text-sm text-slate-500">{note}</p></div>)}</div><div className="mt-10 min-w-0"><ChartCard title="Funding Utilization by Industry"><ResponsiveContainer>{chart.length?<BarChart data={chart}><XAxis dataKey="name"/><YAxis/><Tooltip/><Legend wrapperStyle={{ color: "#cbd5e1", fontSize: 12 }}/><Bar dataKey="committed" name="Committed Cr" fill="transparent" stroke="#22d3ee" strokeWidth={2}/><Bar dataKey="utilized" name="Utilized Cr" fill="transparent" stroke="#fbbf24" strokeWidth={2}/></BarChart>:<BarChart data={data.chartData.impact}><XAxis dataKey="sector"/><YAxis/><Tooltip/><Legend wrapperStyle={{ color: "#cbd5e1", fontSize: 12 }}/><Bar dataKey="impact" name="Funding Signal" fill="transparent" stroke="#fbbf24" strokeWidth={2}/></BarChart>}</ResponsiveContainer></ChartCard></div></div>}
