import { useState } from "react";
import { supportTypes } from "../../data/referenceData.js";
import { useImpactData } from "../../hooks/useImpactData.js";
import { partnershipService } from "../../services/partnershipService.js";
export default function Partnerships(){const { data }=useImpactData();const [done,setDone]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);const [form,setForm]=useState({project_id:"",support_type:supportTypes[0],contribution:"",mentor_name:"",timeline:"",notes:""});const set=(k,v)=>setForm(f=>({...f,[k]:v}));const submit=async(e)=>{e.preventDefault();setBusy(true);setDone("");setError("");try{const payload={...form,project_id:form.project_id||data.projects[0]?.id};const res=await partnershipService.create(payload);setDone(`Partnership saved to MongoDB with status ${res.data?.status || "ACTIVE"}.`)}catch(err){setError(err?.response?.data?.message || err?.response?.data?.detail || "Unable to confirm partnership.")}finally{setBusy(false)}};return <div className="max-w-4xl min-w-0"><h1 className="text-3xl font-semibold tracking-tight text-navy md:text-4xl">Partnership Form</h1>{done&&<p className="mt-6 rounded-2xl bg-green/10 p-4 font-semibold text-green">{done}</p>}{error&&<p className="mt-6 rounded-2xl bg-red-50 p-4 font-semibold text-red-600">{error}</p>}<form onSubmit={submit} className="mt-8 rounded-2xl border bg-white p-6 shadow-sm md:p-8"><div className="grid gap-6 lg:grid-cols-2"><label className="text-sm font-semibold text-slate-600">Project<select value={form.project_id} onChange={e=>set("project_id",e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border px-4 py-3">{data.projects.slice(0,10).map(p=><option key={p.id} value={p.id}>{p.id} - {p.title}</option>)}</select></label><label className="text-sm font-semibold text-slate-600">Support Type<select value={form.support_type} onChange={e=>set("support_type",e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border px-4 py-3">{supportTypes.map(s=><option key={s}>{s}</option>)}</select></label><label className="text-sm font-semibold text-slate-600">Contribution<input required value={form.contribution} onChange={e=>set("contribution",e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border px-4 py-3"/></label><label className="text-sm font-semibold text-slate-600">Mentor Name<input value={form.mentor_name} onChange={e=>set("mentor_name",e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border px-4 py-3"/></label><label className="text-sm font-semibold text-slate-600">Timeline<input value={form.timeline} onChange={e=>set("timeline",e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border px-4 py-3"/></label><label className="text-sm font-semibold text-slate-600 lg:col-span-2">Notes<textarea value={form.notes} onChange={e=>set("notes",e.target.value)} rows="5" className="mt-2 w-full rounded-xl border px-4 py-3"/></label></div><button disabled={busy} className="mt-7 w-full rounded-xl bg-green px-5 py-3 font-semibold text-white disabled:opacity-60 sm:w-auto">{busy?"Saving...":"Confirm Partnership"}</button></form></div>}
