import { useState } from "react";
import { useImpactData } from "../../hooks/useImpactData.js";
import InstituteCard from "../../components/dashboard/InstituteCard.jsx";
export default function Matching(){const { data } = useImpactData(); const [msg,setMsg]=useState("");return <div className="min-w-0"><h1 className="text-3xl font-semibold tracking-tight text-navy md:text-4xl">Matching Module</h1><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-600 md:text-base">Recommended institute cards ranked by expertise, previous projects and availability.</p>{msg&&<p className="mt-6 rounded-2xl bg-green/10 p-4 font-semibold text-green">{msg}</p>}<div className="mt-8 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{data.institutes.map(i=><InstituteCard key={i.name} institute={i} onAssign={()=>setMsg(`${i.name} assigned to the selected challenge.`)}/>)}</div></div>}
